# Telegram-бот для поиска подселения в Минске — MVP-каркас (Этап 2)

**Готовишься к запуску рекламы? Сначала прочитай `DEPLOY.md`** — там
про то, как поднять бота на сервере так, чтобы он работал 24/7, а не
только пока открыт терминал на твоём Mac. Без этого шага реклама
приведёт людей к боту, который не всегда отвечает.

## Автотесты

За время разработки я проверял логику через одноразовые скрипты — они
исчезали после проверки, и если бы кто-то потом что-то поменял и
случайно сломал старую логику, заметить было бы некому. Собрал самое
важное (то, где реально находились баги) в постоянный набор тестов —
105 штук, гоняются одной командой:

```bash
pip install -r requirements-dev.txt   # pytest + pytest-asyncio поверх обычных зависимостей
pytest
```

Что покрыто: matching/score (включая оба реальных бага — «без
подселения» и штраф квартир за пол соседей), извлечение адреса из
Kufar на всех реальных примерах, что накопились за разведку, каскадное
удаление данных, дедупликация объявлений, тихие часы для уведомлений
(включая переход через полночь), склонение числительных, отложенные
уведомления. Тесты используют отдельную временную SQLite-базу на
каждый тест — друг другу не мешают, ничего в реальном `bot.db` не
трогают.

Гоняй `pytest` после любых своих правок в `services/matching.py`,
`sources/kufar.py` или `database/crud.py` — это самые хрупкие места,
там уже были реальные баги.


Этот каркас уже умеет:
- `/start` — регистрация пользователя, главное меню.
- `/profile` — пошаговое заполнение анкеты (пол, возраст, бюджет, районы,
  тип жилья, к кому подселиться, животные/курение/привычки, работа/учёба,
  о себе) и сохранение в SQLite.
- Просмотр анкеты через кнопку «👤 Моя анкета».

Поиска жилья (парсер Kufar, matching, карточки объявлений) пока нет —
это Этапы 3–5.

## Куда что класть

Просто распакуй всё в одну папку — структура уже готова:

```
project/
├── bot.py
├── config.py
├── requirements.txt
├── .env.example
├── .gitignore
├── handlers/       (start.py, profile.py)
├── keyboards/       (main.py, inline.py)
├── states/           (profile.py)
├── database/           (models.py, database.py, crud.py)
└── utils/                (helpers.py)
```

## Установка

```bash
cd project
python3 -m venv .venv
source .venv/bin/activate      # на Windows: .venv\Scripts\activate

pip install -r requirements.txt
```

## Настройка

1. Скопируй `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
2. Открой `.env` и впиши:
   - `BOT_TOKEN` — токен от [@BotFather](https://t.me/BotFather) (команда `/newbot`).
   - `ADMIN_ID` — твой Telegram ID числом (узнать можно у [@userinfobot](https://t.me/userinfobot)).
   - `DB_PATH` — можно оставить `bot.db` (файл создастся сам рядом с `bot.py`).

Пример `.env`:
```
BOT_TOKEN=123456789:AAExampleTokenFromBotFather1234567890
ADMIN_ID=123456789
DB_PATH=bot.db
```

## Запуск

```bash
python3 bot.py
```

Если всё настроено верно, увидишь в логах `Бот запускается...` — значит
бот вышел в поллинг и готов принимать команды в Telegram.

При первом запуске файл `bot.db` создастся автоматически со всеми
таблицами (`users`, `profiles`, `profile_districts`, `listings`,
`favorites` — последние две пока пустые, заполнятся на Этапе 3–4).

## Что проверить у себя

1. Отправь боту `/start` → должно прийти приветствие и меню с кнопками.
2. Нажми «👤 Моя анкета» → бот скажет, что анкеты нет, и предложит `/profile`.
3. Отправь `/profile` → пройди все шаги анкеты до конца.
4. После заполнения снова нажми «👤 Моя анкета» → должна показаться
   сохранённая анкета.
5. Отправь `/profile` ещё раз и заполни по-другому → старая анкета должна
   замениться новой (не задваиваться).

## Известные ограничения MVP (уже учтены в архитектуре, не баги)

- FSM хранится в памяти (`MemoryStorage`) — при перезапуске бота незавершённое
  заполнение анкеты обнулится. Уже сохранённые анкеты в БД не теряются.
- Миграций БД нет (`Base.metadata.create_all`) — если поменяешь модели в
  `database/models.py`, для применения изменений на существующей базе
  проще всего удалить `bot.db` и перезапустить бота (данные потеряются).
  Alembic можно добавить позже, когда схема стабилизируется.
- `/update` и `/stats` для администратора появятся на Этапе 6.

## Дальше — Этап 3 (сделано)

Разведка Kufar проведена, `sources/kufar.py` написан, найденный баг с
пропущенным районом исправлен (см. историю выше в README, если нужно).

## Этап 4–5 — matching и поиск в боте (сделано)

Кнопка «🏠 Найти жильё» теперь работает по-настоящему:
1. Бот берёт районы из твоей анкеты.
2. Идёт на Kufar через `sources/kufar.py` (по одному запросу на район,
   без нарушения `robots.txt`).
3. Сохраняет найденное в БД (`services/listings.py`).
4. Прогоняет через фильтр и рейтинг (`services/matching.py`) — жёстко
   отсекается только то, что точно дороже бюджета; остальное сортируется
   по проценту соответствия.
5. Показывает карточки одну за другой с кнопками ⬅️/➡️, ❤️ (сохранить в
   избранное — уже работает по-настоящему, не заглушка) и 🔗 на само
   объявление.

### Что нужно сделать у себя

1. Зависимости не менялись с прошлого раза — если уже стоят, ничего
   доустанавливать не надо.
2. **Если у тебя уже есть файл `bot.db`** — удали его перед запуском:
   схема снова поменялась (у `price` в объявлениях убрали обязательность,
   иначе бот падал бы на любой «Договорной» цене — нашёл это в тестах,
   уже исправлено). Миграций всё ещё нет, поэтому проще всего:
   ```bash
   rm bot.db
   ```
3. Запусти бота: `python3 bot.py`
4. В Telegram: `/start` → `/profile` (если анкеты ещё нет или хочешь
   пересоздать) → нажми **🏠 Найти жильё**.
5. Бот напишет «Ищу подходящие варианты…», через несколько секунд должна
   появиться первая карточка с процентом соответствия. Полистай ⬅️➡️,
   попробуй ❤️ (нажми ещё раз на ту же карточку — должно переключиться на
   «убрать из сохранённых»).

Если результатов ноль, или бот падает с ошибкой, или карточка выглядит
криво (обрезанный текст, мусор в заголовке) — пришли мне, что вышло, я
это не мог до конца проверить сам: в моей песочнице Kufar отвечает 403
(похоже, из-за дата-центрового IP), так что реальный прогон запроса к
Kufar возможен только с твоей стороны.

### Известные грубые места (сознательно, не баги)

- Заголовок карточки — черновая эвристика (обрезанный слипшийся текст),
  see `sources/kufar.py`. Как увидим реальный вывод — доточим.
- Курс BYN→USD захардкожен приблизительно в `services/matching.py`
  (`BYN_TO_USD_RATE`). Обновляй вручную при сильных отклонениях от жизни.
- Тип жилья (комната/подселение) и пол соседей определяются по ключевым
  словам в тексте — это эвристика, не точные данные, поэтому участвуют
  только в рейтинге (`⭐ %`), не в жёстком отборе.

### Дальше — Этап 6

`/update` и `/stats` для администратора, уведомления о новых подходящих
объявлениях.

## Правки по твоей обратной связи (после первого реального теста)

Спасибо за реальный вывод карточки — по нему поправил:

- **Убрал мусор** «Позвонить Сравнить» / «calculator» — раньше он просто
  подмешивался в текст объявления, теперь чистится в одном месте
  (`KufarSource._clean_text`, `sources/kufar.py`).
- **Убрал дублирование** — раньше заголовок и описание были почти
  одинаковым текстом (оба — весь слипшийся блок). Теперь описание
  показывает только то, что идёт ПОСЛЕ заголовка (`services/formatting.py`).
- **Добавил фото** — если у объявления нашлась картинка (`<img>` внутри
  карточки на Kufar), бот теперь присылает её как `send_photo` с текстом
  в подписи, а не отдельным текстовым сообщением. Если картинки нет —
  как и раньше, просто текстом. Из-за этого пагинация (⬅️➡️) больше не
  редактирует сообщение на месте, а удаляет старую карточку и присылает
  новую — Telegram не даёт превратить фото-сообщение в текстовое через
  `edit_*` и наоборот.

**Не стал делать сейчас — дата публикации.** У меня нет достоверных
данных, где именно на странице Kufar она лежит (я не видел реальную
разметку, только текст карточки-ссылки) — гадать по тексту не хочу,
рискую подсунуть неверную дату. Если хочешь, можем разобраться отдельно:
открой в браузере DevTools на любой карточке `re.kufar.by/l/minsk/...`
и посмотри, есть ли рядом с объявлением что-то вроде «сегодня», «вчера»,
конкретная дата — если да, пришли мне, как это выглядит в HTML (Ctrl+Shift+C
→ клик по этому тексту → посмотри на подсвеченный тег), и я добавлю
извлечение.

### Что нужно сделать у себя

Код поменялся, БД — нет (новых полей в модели не добавлял). Просто:
```bash
python3 bot.py
```
и снова нажми «🏠 Найти жильё».

## Этап 6 — админка (сделано)

Добавил `/update` и `/stats`, доступны только тебе (по `ADMIN_ID` из `.env`
— остальным бот на эти команды просто не ответит, как будто их не существует).

- **`/update`** — обходит все 9 официальных районов Минска сразу (не
  только те, что в чьей-то анкете) и складывает найденное в БД. Одно
  сообщение "Обновляю..." → через ~10-15 секунд (9 запросов с паузой
  между ними, как и раньше — без нарушения `robots.txt`) → "Готово,
  обработано N объявлений".
- **`/stats`** — пользователи всего, активные за 7 дней, анкеты,
  объявления в базе, избранное.

Про «активных за 7 дней» — важная деталь: раньше `updated_at` у
пользователя обновлялся только если реально менялись `username`/
`first_name`, то есть почти никогда после первого `/start`. Метрика была
бы бессмысленной. Поправил — теперь `updated_at` трогается при каждом
обращении к боту, независимо от того, поменялось что-то в профиле или
нет.

Заодно поймал и убрал `DeprecationWarning` от `datetime.utcnow()` — у
тебя Python 3.14, там это уже официально помечено к удалению в будущем.
Проверил тестом с `-W error::DeprecationWarning`, чтобы быть уверенным,
что нигде больше не осталось.

### Что нужно сделать у себя

БД снова не менялась. Просто:
```bash
python3 bot.py
```
В Telegram (с того аккаунта, чей ID указан как `ADMIN_ID` в `.env`)
отправь `/update`, потом `/stats`.

## Раздел «Сохранённые» (сделано)

Кнопка «❤️ Сохранённые» теперь реально показывает то, что ты сохранял —
переиспользует ту же карточку/пагинацию, что и поиск (это тот же
`SearchSession.browsing`, просто список объявлений собирается не через
Kufar+matching, а прямо из таблицы `favorites`).

Одна деталь, которую специально добавил сверх минимума: если убрать ❤️
с карточки **находясь именно в разделе «Сохранённые»** — она сразу
пропадает из текущего списка (не просто меняется кнопка на «Сохранить»
обратно). Если убрать последнюю — бот вежливо сообщит, что сохранённых
больше нет, и вернёт в меню. При обычном поиске (не в «Сохранённых»)
поведение как было: сняли ❤️ — карточка остаётся в списке, просто кнопка
меняется обратно на «Сохранить».

### Что нужно сделать у себя

БД не менялась. `python3 bot.py`, попробуй сохранить пару объявлений во
время поиска, потом зайди в «❤️ Сохранённые» и убери одно — должно сразу
исчезнуть из списка, не дожидаясь повторного открытия раздела.

## Version 1.1 — прозрачный score + быстрый поиск (сделано)

По итогам продуктового аудита — два изменения:

### Карточка объясняет процент

Раньше «⭐ 95%» просто появлялось из ниоткуда. Теперь под ним — чек-лист:
```
⭐ Подходит вам: 95%
✅ Бюджет · ✅ Район · ⚠️ Пол соседей не уточнён · ✅ Тип жилья
```
Это те же веса, что считались и раньше (`services/matching.py`), просто
теперь `explain_match()` возвращает не голое число, а разбивку по
критериям — `match_score()` остался как обёртка для мест, где нужно
только число. Обрати внимание на формулировку «не уточнён», а не
«не подходит» — там, где наша эвристика не смогла ничего подтвердить,
честнее показать неопределённость, чем выдумывать однозначный минус.

Заодно (это давно откладывалось) — довёл до конца извлечение адреса из
слипшегося текста Kufar: в шапке карточки теперь именно улица
(«Газеты Правда пр, 22»), а не обрывок «действие+адрес+начало описания».
Нашёл по пути тонкий баг в первой версии регулярки: символьный класс
`[А-Яа-яЁё]` не различает регистр *внутри* одного слова, поэтому склеенные
без пробела фрагменты вроде «МинскСтахановская» съедались целиком как одно
слово. Сузил класс для середины слова до строчных букв — заглавная сама
стала границей. Проверено на всех реальных примерах, что накопились за
разведку.

### Быстрый поиск без анкеты

Если нажать «🏠 Найти жильё» без заполненной анкеты — раньше бот просто
отправлял на `/profile`. Теперь предлагает выбор:

```
⚡ Быстрый поиск       👤 Заполнить анкету
```

Быстрый поиск — это 3 вопроса (бюджет / районы / кого ищешь в соседи) и
сразу результаты, без сохранения анкеты в БД. Технически — `QuickProfile`
в `services/matching.py`: лёгкий dataclass с теми же именами полей, что
использует matching-логика, поэтому `match_listing`/`explain_match`
работают с ним точно так же, как с настоящим `Profile` — без отдельной
ветки кода.

Карточки в «Сохранённых» теперь **без** процента подходящести — раньше
score там пересчитывался на лету против текущей анкеты, что для объявлений
из быстрого поиска (анкеты вообще нет) было бы либо ошибкой, либо
бессмысленным числом. Явное решение: в контексте «уже сохранил» процент
не нужен, там важен сам факт, что понравилось.

Также добавил «📋 X из Y» на каждую карточку — теперь видно, сколько всего
найдено/сохранено, не только в момент открытия раздела, а на протяжении
всего пролистывания.

### Что нужно сделать у себя

БД не менялась. `python3 bot.py`. Попробуй:
1. Без анкеты нажать «🏠 Найти жильё» — должен появиться выбор быстрый/анкета.
2. Пройти быстрый поиск — 3 шага, сразу карточки с чек-листом.
3. Проверить обычный поиск (если анкета уже есть) — чек-лист там тоже
   должен появиться.
4. Проверить «❤️ Сохранённые» — там теперь счётчик сверху и карточки без
   процента.

## «⚙️ Настройки» (сделано)

Раньше кнопка честно писала «будет добавлено позже» — теперь работает:

- **✏️ Изменить анкету** — то же самое, что `/profile`, просто доступно
  из меню, не только командой.
- **🗑 Удалить анкету** — с подтверждением («Точно?» / Да / Отмена).
  Удаляется только анкета — избранное (❤️) не трогается, это специально
  проверено тестом (см. ниже).

Технически `crud.delete_profile()` использует тот же приём, что и
`upsert_profile()` раньше: явный `DELETE` по districts вместо обхода
ORM-коллекции — иначе рисковали снова поймать `MissingGreenlet` в async-сессии
(это тот самый баг с самого начала проекта, теперь паттерн закреплён и
переиспользуется, а не повторяется вручную).

### Что нужно сделать у себя

БД не менялась. Зайди в «⚙️ Настройки», попробуй изменить анкету, потом
удалить её и убедиться, что «❤️ Сохранённые» после этого не опустели.

## Правки по твоей обратной связи (2)

### Баг с «Отмена» в настройках — исправлено

Нажатие «Отмена» при удалении анкеты раньше просто писало «Хорошо, ничего
не трогаю» и оставляло тебя в тупике — без кнопок назад. Теперь возвращает
экран настроек с теми же кнопками (✏️ Изменить / 🗑 Удалить), а
подтверждение «ничего не трогаю» показывается всплывающим уведомлением
(тем самым, что коротко мигает сверху), не отдельным сообщением.

### Поиск квартир — добавлено

Раньше бот искал только по категории «Комнаты» на Kufar. Квартиры —
это не просто ещё один фильтр внутри той же категории, а **отдельная
категория Kufar** со своим URL (`/snyat/kvartiru` вместо `/snyat/komnatu`).
Проверил это вживую перед тем как писать код (а не по памяти) — заодно
подтвердил все 9 слагов районов напрямую со страницы фильтров Kufar:
раньше часть из них были только предположением по паттерну транслитерации,
теперь все 9 — 100% подтверждены.

Что изменилось:
- В анкете (`/profile`) в шаге «Что ищешь?» появилась кнопка «Квартира».
- Матчинг по типу жилья для квартир теперь **достоверный, не эвристика**:
  мы точно знаем, из какой категории Kufar пришло объявление, в отличие
  от комнаты/подселения (там по-прежнему определяем по тексту, Kufar их
  не различает структурно).
- Если анкета говорит «Неважно» — бот ищет **обе** категории сразу (в два
  раза больше запросов к Kufar, но всё ещё в рамках `robots.txt` и с той
  же паузой между запросами).
- `/update` у администратора теперь тоже обходит обе категории по всем
  районам — дольше по времени (примерно вдвое), но полнее.
- Быстрый поиск (`/quicksearch`) пока не спрашивает про тип жилья — по
  умолчанию ищет везде («Неважно»), чтобы не удлинять и без того короткий
  флоу. Если захочешь, чтобы спрашивал — скажи, добавлю четвёртым шагом.

Заодно, пока тестировал офлайн, поймал **отдельный** баг — не в новом
коде, а в самой логике определения подселения: фраза «без подселения»
(отрицание) засчитывалась как «есть подселение», потому что поиск шёл по
подстроке «подсел» без учёта отрицания. Поправил — теперь «без подсел…»
явно распознаётся и не считается совпадением.

### Что нужно сделать у себя

БД не менялась (поле `housing_type` у объявлений уже существовало в схеме,
просто раньше не заполнялось). `python3 bot.py`, попробуй в анкете
выбрать «Квартира» и поискать — а также глянь `/quicksearch`, он всё ещё
ищет и комнаты, и квартиры вместе.

## Правки по твоей обратной связи (3)

### Логическая ошибка в анкете — исправлено

При выборе «Квартира» бот всё равно спрашивал «К кому готов(а)
подселиться?» — бессмысленный вопрос для того, кто снимает квартиру
целиком, а не подселяется к соседям. Теперь при «Квартира» этот шаг
пропускается полностью, вопрос про животных идёт сразу следом, а пол
соседей автоматически проставляется в «неважно» (это ни на что не влияет
при матчинге — там для квартир и так используется достоверная категория
Kufar, а не пол соседей). Для «Комната»/«Подселение» вопрос как и раньше
задаётся.

### Приветствие — исправлено

`/start` теперь упоминает квартиру: «помогу найти подселение, комнату
или квартиру в Минске» — раньше квартира была тихо забыта в тексте, хотя
уже появилась в самой функциональности.

### Что нужно сделать у себя

БД не менялась. `python3 bot.py`, пройди `/profile` заново, выбери
«Квартира» — следующим шагом должно спросить про животных, а не про
соседей.

## Правка по своей инициативе — нашёл при повторной проверке

После добавления квартир перечитал `explain_match()` ещё раз и нашёл
логическую нестыковку в самом скоринге (не в UI — в самих цифрах): «Пол
соседей» считался эвристикой по тексту объявления для АБСОЛЮТНО ЛЮБОГО
объявления, включая квартиры. В объявлениях о квартирах пол почти никогда
не упоминается (там не подселяются к кому-то) — то есть при поиске
«неважно» квартиры систематически теряли 20 баллов из 100 просто потому,
что вопрос к ним неприменим, а не потому что они реально не подходили.

Исправил: если объявление — квартира, «пол соседей» больше не участвует
в скоринге как штраф (засчитывается автоматически, ничего не отнимая).
В чек-листе карточки эта строка для квартир теперь вообще не показывается
— незачем показывать «✅ Пол соседей», если это не сигнал, а автоматическая
заглушка. Проверил на реальном сравнении: комната без указания пола честно
получает «⚠️ Пол соседей не уточнён» (эвристика по-прежнему работает там,
где применима), а квартира просто не поднимает эту тему вообще.

### Что нужно сделать у себя

БД не менялась. Если будешь искать «Неважно» по типу жилья с указанным
предпочтением по полу соседей — квартиры в выдаче теперь не должны
незаслуженно проседать по проценту.

## 🔔 Уведомления (Version 1.2, сделано)

Добавлена новая зависимость — `apscheduler`, ей нужно доустановиться:
```bash
pip install -r requirements.txt
```

Новая таблица `notification_subscriptions` — БД пересоздастся сама при
следующем запуске (`Base.metadata.create_all` просто добавит недостающую
таблицу, существующие данные не тронет — миграция не нужна, можно не
удалять `bot.db`).

### Как это работает

- Новая кнопка в меню — **🔔 Уведомления**, вкл/выкл одним тапом.
- Работает **только по сохранённой анкете** (не по быстрому поиску — там
  нет постоянных критериев для повторной проверки в фоне). Если анкеты
  нет — бот вежливо попросит сначала пройти `/profile`.
- Каждые **20 минут** (если есть хоть одна активная подписка — если
  подписчиков нет, бот вообще не трогает Kufar лишний раз) фоновая задача:
  1. Обновляет базу по всем районам и категориям — **один раз на всех
     подписчиков сразу**, не по отдельному запросу на каждого (иначе
     нагрузка на Kufar росла бы линейно с числом пользователей).
  2. Для каждой подписки ищет объявления, которых не было в базе на
     момент прошлой проверки (`Listing.parsed_at` не трогается при
     повторных апсертах — надёжная метка "увидели впервые").
  3. Прогоняет через тот же matching, что и обычный поиск — уведомляет
     только про то, что действительно подходит (порог по умолчанию 70%).
  4. Не больше 3 уведомлений за один цикл на пользователя — чтобы не
     устроить спam, если сразу набралось много.
- При включении подписки не шлёт разом всё, что уже есть в базе — только
  то, что появится **с этого момента**. Иначе первое включение выглядело
  бы как спам-бомбардировка.
- Если пользователь заблокировал бота — уведомление не долетит с ошибкой
  `TelegramForbiddenError`, и бот сам отключит его подписку, чтобы не
  дёргать впустую на каждом цикле.

### Что осознанно упростил (сравнение с полным дизайном из аудита)

- **Без quiet hours** — уведомления могут прийти в любое время суток.
  Добавить не сложно, но не делал заранее, пока неясно, действительно ли
  это нужно.
- **Порог score (70%) — захардкожен**, не настраивается пользователем
  через UI. Поле `min_score` в таблице уже есть, просто интерфейса для
  его смены пока нет — техническая часть готова, если понадобится.
- **Одна подписка на пользователя**, не несколько с разными параметрами.

### Что не смог протестировать сам

Логику «кому что слать» проверил офлайн полностью — новое/старое по
времени, жёсткий фильтр, порог score, авто-отключение при удалённой
анкете, идемпотентность вкл/выкл. А вот **реальную отправку в Telegram и
живой запуск шедулера** — нет, как и раньше, сеть до `api.telegram.org`
в моей песочнице недоступна. Технически, чтобы увидеть уведомление живьём,
нужно либо подождать 20 минут при активной подписке и новых объявлениях
на Kufar, либо (для быстрой проверки) временно уменьшить
`CHECK_INTERVAL_MINUTES` в `services/notifications.py` на 1 минуту,
поймать уведомление и вернуть обратно.

### Что нужно сделать у себя

```bash
pip install -r requirements.txt
python3 bot.py
```
Зайди в «🔔 Уведомления», включи. Для быстрой проверки — временно поставь
`CHECK_INTERVAL_MINUTES = 1` в `services/notifications.py`, подожди
минуту-две (если на Kufar в твоём районе появится что-то новое и
подходящее), потом верни значение на 20.

## Превентивное усиление БД (не подтверждённый баг, но обоснованный риск)

С появлением фонового шедулера конкурентный доступ к SQLite перестал быть
гипотетическим: раньше в базу писали только по одной операции за раз
(реакция на действия пользователя), теперь есть процесс, который лезет
туда независимо каждые 20 минут — вперемешку с обычным поиском или
ручным `/update`.

Добавил в `database/database.py` две PRAGMA при подключении:
- `journal_mode=WAL` — позволяет читать базу во время записи (дефолтный
  режим SQLite это не гарантирует так же хорошо);
- `busy_timeout=5000` — явно прописанное ожидание перед отказом, вместо
  того чтобы полагаться на неявный дефолт драйвера.

**Важная честная оговорка**: я прогнал стресс-тест (5 параллельных
писателей по 20 записей одновременно) и на конфигурации БЕЗ этой правки
он тоже прошёл без единой ошибки — то есть я не смог воспроизвести
реальный сбой, только обосновал риск теоретически (у `aiosqlite`,
похоже, и так есть свой дефолтный таймаут). Оставляю правку как
разумную превентивную меру, а не как «нашёл и исправил баг» — это разные
по силе заявления, и я не хочу путать один с другим.

### Что нужно сделать у себя

БД не требует пересоздания — PRAGMA применяются на каждое подключение
автоматически, это не изменение схемы.

## Правки по твоей обратной связи (4)

### Уведомления — нашёл и исправил настоящий баг

Если в анкете выбран **«Любой район»**, поиск раньше делал ОДИН запрос по
всему городу без привязки к конкретному району — и такие объявления
получали `district = None`. Это ломало сразу две вещи:

1. **Score в обычном поиске** — `district_ok` для таких пользователей
   был всегда `False`, даже если район объективно подходил (просто мы
   его не записали).
2. **Уведомления вообще ничего не находили** — фильтр там искал
   `Listing.district == "any"`, а такого значения не бывает физически
   (только `None` или настоящий слаг района). Для профилей с «Любой
   район» уведомления были гарантированно сломаны — не иногда, а всегда.

Если у тебя в анкете выбраны конкретные районы (не «Любой») — это была
не твоя причина, и здесь может быть проще: планировщик проверяет раз в
**20 минут**, и только с этого момента считает время «до последней
проверки» — если тестировал меньше 20 минут, уведомление просто ещё не
успело бы прийти при любых раскладах. Для быстрой проверки — как и
раньше, можно временно поставить `CHECK_INTERVAL_MINUTES = 1` в
`services/notifications.py`.

Исправление: при «Любой район» поиск теперь идёт по всем 9 настоящим
районам по отдельности (как и `/update` у админа), а не одним
неразличимым городским запросом — у каждого объявления всегда будет
настоящий район. Уведомления заодно получили страховку: если строка
`"any"` всё же попадёт в фильтр — район просто не будет ограничиваться,
вместо того чтобы искать несуществующее значение.

Проверил сам весь сценарий целиком: воспроизвёл баг (подтвердил, что
уведомления для «Любой район» находили 0 совпадений), применил фикс,
убедился, что и score, и уведомления начали работать как надо.

### Дата объявления — добавлена, но честно про её природу

Настоящую дату публикации на Kufar я так и не смог достоверно вытащить
(писал об этом раньше — не видел живую разметку с ней, гадать по тексту
не хотел). Но у нас уже есть кое-что честное: `Listing.parsed_at` —
момент, когда **мы сами** впервые увидели объявление в своей базе. Это
не то же самое, что «когда оно появилось на Kufar», но реальные данные,
не выдумка — и раз база теперь обновляется регулярно (уведомления каждые
20 минут плюс обычные поиски), это разумный прокси свежести.

В карточке появилась строка:
```
🕐 Заметили у себя: 3 ч назад
```
(или «вчера», «5 дн назад», конкретная дата — если давно). Формулировка
специально «заметили у себя», а не «опубликовано» — чтобы не выдавать
это за то, чем оно не является.

### Что нужно сделать у себя

БД не менялась. `python3 bot.py`. Попробуй поставить в анкете «Любой
район» и запустить поиск/уведомления ещё раз — теперь должно находить.

## Найденная попутно системная проблема — и то, что на ней построено

Пока разбирался с уведомлениями, проверил ещё кое-что, что давно не
тестировал: работает ли вообще `ON DELETE CASCADE` в моделях. Оказалось —
**нет, никогда не работал**. У SQLite проверка внешних ключей по
умолчанию выключена, даже если в схеме честно написано
`ondelete="CASCADE"` — это не баг SQLAlchemy, известная особенность
самого SQLite. Мы просто никогда явно не включали `PRAGMA foreign_keys`.

Практически это ничего не ломало **до сих пор**, потому что
единственное существующее удаление (`delete_profile`, в Настройках)
всегда чистило districts вручную, не полагаясь на каскад. Но это
значило, что каскад был бесполезной строчкой в коде — просто ничего не
делал на уровне самой БД.

Включил `PRAGMA foreign_keys=ON` в `database/database.py`. Пересоздавать
`bot.db` не нужно — сама схема (DDL) всегда была правильной, каскад был
объявлен с самого начала проекта, просто не проверялся. Прогнал и
широкий регрессионный тест (анкета/объявления/избранное/уведомления/
статистика — ничего не сломалось), и отдельный тест именно каскада:
удалил пользователя напрямую (чего раньше в коде никогда не делали) —
анкета, её районы, избранное и подписка на уведомления удалились сами,
без единой ручной строчки `DELETE`.

## ⚠️ Полное удаление данных + `/privacy` (сделано, раз каскад теперь реально работает)

Раз каскад заработал по-настоящему, это открыло возможность добавить то,
что стоило сделать перед рекламой (см. предыдущий чек-лист): реальное
самообслуживание по приватности, а не только «удалить анкету».

- **Настройки → ⚠️ Удалить все мои данные** — с явным предупреждением и
  подтверждением. Удаляет пользователя целиком: анкету, районы,
  избранное, подписку на уведомления — используя тот самый каскад,
  без единой ручной строчки очистки (раньше бы пришлось писать вручную
  под каждую таблицу, как в `delete_profile`).
- **`/privacy`** — короткая честная памятка: что храню, кому не передаю
  (данные остаются в моей базе, на Kufar уходят только сами поисковые
  запросы, не личные данные), как удалить — с прямыми ссылками на обе
  опции в Настройках.

Проверил полным сценарием: анкета + избранное + активная подписка →
удаление всего → анкеты нет, избранного нет, подписки нет, а сами
объявления в общей базе (`listings`) остаются нетронутыми — это общие
данные, не привязанные к конкретному пользователю, удалять их не за чем.

### Что нужно сделать у себя

БД не менялась. `python3 bot.py`, попробуй `/privacy` и «⚠️ Удалить все
мои данные» в Настройках.

## Тихие часы для уведомлений (сделано по своей инициативе)

В аудите я сам пометил отсутствие тихих часов как «сознательно
упрощено» — но чем ближе к рекламе и реальным людям, тем это рискованнее:
одно уведомление в 3 ночи — и человек блокирует бота навсегда, теряем
подписчика без права на ошибку. Добавил простой фиксированный дефолт,
без сложного UI под настройку на пользователя (это можно добавить
позже, если реально понадобится):

**С 23:00 до 08:00 по Минску весь цикл проверки пропускается целиком** —
включая поход в Kufar, не только отправку. Смысла тратить запросы ночью
нет, если разослать всё равно нельзя. То, что накопится за ночь, никуда
не потеряется — `last_checked_at` для пропущенных подписок не трогается,
и первый же цикл после 08:00 найдёт всё честно, просто с задержкой, а не
мгновенно ночью.

Проверил границы (22:59/23:00/00:00/07:59/08:00 и обычный день) — все
десять контрольных точек сошлись, включая переход через полночь, который
проще всего сломать в такой логике. Отдельно проверил и то, что
`run_notification_cycle` в тихие часы реально не трогает БД вообще (не
просто не шлёт сообщения) — через подмену функции проверил, что
`get_active_subscriptions` в это время не вызывается ни разу.

### Что нужно сделать у себя

БД не менялась, зависимости не менялись. Просто `python3 bot.py` — эту
часть в реальном времени не проверить без ожидания смены суток, но
логика протестирована на всех граничных значениях времени.

## Уведомления больше не «вываливают» карточки (сделано)

Раньше при срабатывании цикла бот сразу слал до 3 полноценных карточек
подряд, одну за другой — то самое ощущение дампа. Теперь сначала одно
короткое сообщение:

```
🔔 Нашёл для тебя 2 новых варианта — показать?
[Да, показать]  [Не сейчас]
```

При «Да» — открывается тот же просматриваемый список с пагинацией
(⬅️➡️❤️🔗🏠), что и в обычном поиске, а не отдельный менее удобный способ
показа. При «Не сейчас» — просто ничего не показывается (сами объявления
никуда не теряются, их всегда можно найти обычным поиском).

Технически интересная часть: фоновый цикл уведомлений работает вне
обычного потока обработки сообщений — у него нет "текущего" chat/callback,
через который обычно приходят данные в FSM (машину состояний диалога).
Чтобы кнопка «Показать» могла переиспользовать готовый механизм карточек,
я формирую тот же ключ, каким сам aiogram адресовался бы к пользователю
в его личном чате (`bot_id` + `chat_id` + `user_id`, для личных чатов
`chat_id == user_id`), и пишу состояние напрямую туда — до того, как
пользователь вообще увидел сообщение. Когда он нажимает «Показать»,
обычный обработчик коллбэка видит ровно то же состояние, будто это он
сам его туда положил.

Это единственное место в проекте, где я не был до конца уверен, что
сработает, не увидев своими глазами — поэтому проверил отдельно и
дотошно: сначала само по себе (записал состояние «извне», прочитал
«изнутри» — совпало), потом весь путь целиком с подменённым
`bot.send_message` (нашлось 2 подходящих объявления → отправилось ровно
одно сообщение-сводка с правильным склонением «2 новых варианта» → в FSM
лежат оба ID и объяснения score). Реальную доставку в Telegram, как и
раньше, проверить не могу — сети до `api.telegram.org` у меня нет.

### Что нужно сделать у себя

БД не менялась. `python3 bot.py`. Для проверки — как обычно, можно
временно выставить `CHECK_INTERVAL_MINUTES = 1` в
`services/notifications.py`, дождаться срабатывания и нажать «Да,
показать» на сводке.

## Нашёл и исправил race condition в том, что сам только что сделал

Перечитывая предыдущую правку, заметил реальную уязвимость: фоновый цикл
уведомлений писал состояние диалога (FSM) напрямую в чужой контекст —
работало, я это проверил, но не учитывал один сценарий. **Что, если в
момент срабатывания цикла пользователь как раз находится в середине
`/profile` или `/quicksearch`?** Уведомление молча подменило бы его
текущее состояние диалога на "просмотр карточек" — следующий ответ на
вопрос анкеты попал бы уже не туда, а сам процесс заполнения тихо
сломался бы без всякого объяснения.

Исправил архитектурно, не заплаткой: находки теперь сохраняются в БД
(новая таблица `pending_notifications`), а не в FSM. Состояние диалога
трогается только в хендлере кнопки «Показать» — то есть строго в ответ
на осознанный клик самого пользователя, никогда из фона. Заодно это
упростило код — больше не нужен трюк с ручной сборкой `StorageKey` и
проброс `storage` через весь путь от `bot.py` до планировщика, он
полностью убран.

Проверил именно то, из-за чего затеял правку: посадил "пользователя" на
середину заполнения анкеты (шаг "районы", часть ответов уже дана),
запустил фоновый цикл так, чтобы он реально что-то нашёл — и убедился,
что состояние и данные анкеты остались нетронутыми, а находка сохранилась
отдельно в БД. Потом досимулировал: пользователь доделал анкету, потом
сам нажал «Показать» — вот только тогда состояние перешло в просмотр
карточек, как и должно быть.

### Что нужно сделать у себя

Новая таблица `pending_notifications` создастся сама при следующем
запуске, `bot.db` удалять не нужно. Просто `python3 bot.py`.

















